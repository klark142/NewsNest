<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Items List</title>
</head>
<body>
    <h1>News items: </h1>
    <ul>
        <% items.forEach(function(item) { %>
            <li><%= item.name %>: <%= item.desctiption %></li>
            
        <% }); %>
    </ul>
</body>
</html>